// Objeto Math
let resultado;
resultado = Math.PI;  //Para saber el valor de pi.
resultado = Math.round(2.5);  //Para redondear los valores tomando el decimal si esta por encima o debajo de 0.5
resultado = Math.ceil(2.5);  //Para redondear hacia arriba 
resultado = Math.floor(2.5);  //Para redondear hacia abajo 
resultado = Math.sqrt(25);  //Para sacar ka raiz cuadrada
resultado = Math.abs(-25);  //Para dar el valor absoluto en positivo
resultado = Math.min(4, 5, 7, 9, 3, 5);  //Para trae el valor minimo de la lista
resultado = Math.max(4, 5, 7, 9, 3, 5);  //Para trae el valor maximo de la lista
resultado = Math.random(); //Para traer un numero alearotio. Puro 0,9 para abajo sin llegar a 1.
resultado = Math.floor(Math.random() * 30); //Para traer un numero alearotio que no pase de 30

console.log(resultado);